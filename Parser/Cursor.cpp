#include "Cursor.hpp"

void UEP::Cursor::position(const std::string& accumulatedString, std::string& sequence, std::string& buffer, size_t& index, UEPColourSequence& currentColour, ParserInfo& info)
{
    if (sequence == "\x1B[H")
        ;
    else
        ;
}

void UEP::Cursor::up(const std::string& accumulatedString, std::string& sequence, std::string& buffer, size_t& index, UEPColourSequence& currentColour, ParserInfo& info)
{
    for (const auto& a : sequence)
    {

    }
}

void UEP::Cursor::down(const std::string& accumulatedString, std::string& sequence, std::string& buffer, size_t& index, UEPColourSequence& currentColour, ParserInfo& info)
{
    for (const auto& a : sequence)
    {

    }
}

void UEP::Cursor::right(const std::string& accumulatedString, std::string& sequence, std::string& buffer, size_t& index, UEPColourSequence& currentColour, ParserInfo& info)
{
    for (const auto& a : sequence)
    {

    }
}

void UEP::Cursor::left(const std::string& accumulatedString, std::string& sequence, std::string& buffer, size_t& index, UEPColourSequence& currentColour, ParserInfo& info)
{
    for (const auto& a : sequence)
    {

    }
}

void UEP::Cursor::lineBeginDown(const std::string& accumulatedString, std::string& sequence, std::string& buffer, size_t& index, UEPColourSequence& currentColour, ParserInfo& info)
{
    for (const auto& a : sequence)
    {

    }
}

void UEP::Cursor::lineBeginUp(const std::string& accumulatedString, std::string& sequence, std::string& buffer, size_t& index, UEPColourSequence& currentColour, ParserInfo& info)
{
    for (const auto& a : sequence)
    {

    }
}

void UEP::Cursor::oneLineUp(const std::string& accumulatedString, std::string& sequence, std::string& buffer, size_t& index, UEPColourSequence& currentColour, ParserInfo& info)
{
    for (const auto& a : sequence)
    {

    }
}

void UEP::Cursor::saveCursorPosition(const std::string& accumulatedString, std::string& sequence, std::string& buffer, size_t& index, UEPColourSequence& currentColour, ParserInfo& info)
{

}

void UEP::Cursor::restoreCursorPosition(const std::string& accumulatedString, std::string& sequence, std::string& buffer, size_t& index, UEPColourSequence& currentColour, ParserInfo& info)
{

}

void UEP::Cursor::toColumn(const std::string& accumulatedString, std::string& sequence, std::string& buffer, size_t& index, UEPColourSequence& currentColour, ParserInfo& info)
{

}

void UEP::Cursor::requestCursorPosition(const std::string& accumulatedString, std::string& sequence, std::string& buffer, size_t& index, UEPColourSequence& currentColour, ParserInfo& info)
{

}